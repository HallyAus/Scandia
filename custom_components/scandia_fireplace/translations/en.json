{
  "config": {
    "step": {
      "user": {
        "title": "Sign in to Tuya / Smart Life",
        "description": "Enter the **user code** from your Smart Life (or Tuya Smart) app. Find it in the app under **Me → ⚙ (Settings) → Account and Security → User Code**. No developer account is needed.",
        "data": {
          "user_code": "User code"
        }
      },
      "scan": {
        "title": "Scan to link your account",
        "description": "Open the **Smart Life** app, tap **＋ (top right) → Scan**, and scan this QR code. Then press **Submit** below to finish."
      },
      "select_device": {
        "title": "Select your fireplace",
        "description": "Choose the fireplace from the devices on your account.",
        "data": {
          "device_id": "Fireplace"
        }
      }
    },
    "error": {
      "login_error": "Login failed. Check the user code, make sure you scanned the QR code with the Smart Life app, and try again.",
      "temp_range": "The minimum temperature must be lower than the maximum temperature."
    },
    "abort": {
      "already_configured": "This fireplace is already configured.",
      "no_devices": "No devices were found on this account. Add the fireplace to the Smart Life app first.",
      "reauth_successful": "Successfully re-authenticated."
    }
  },
  "options": {
    "error": {
      "temp_range": "The minimum temperature must be lower than the maximum temperature."
    },
    "step": {
      "init": {
        "title": "Scandia fireplace function codes",
        "description": "Map the Tuya function codes to fireplace functions. Leave an optional field blank to disable that feature. Download the integration's diagnostics to see the codes your device actually reports.",
        "data": {
          "code_power": "Power / flame on-off code",
          "code_heat": "Heating element code",
          "code_target_temp": "Target temperature code",
          "code_current_temp": "Current temperature code",
          "code_flame_brightness": "Flame brightness code",
          "code_flame_effect": "Flame effect / colour code",
          "code_flame_speed": "Flame speed code",
          "code_timer": "Countdown timer code",
          "code_child_lock": "Child lock code",
          "code_preset": "Heat preset code (eco/comfort/boost)",
          "code_power_w": "Power sensor code (watts)",
          "code_energy": "Energy sensor code (kWh)",
          "min_temp": "Minimum target temperature (°C)",
          "max_temp": "Maximum target temperature (°C)"
        }
      }
    }
  },
  "entity": {
    "light": {
      "flame": { "name": "Flame" }
    },
    "select": {
      "flame_speed": { "name": "Flame speed" }
    },
    "number": {
      "timer": { "name": "Auto-off timer" }
    },
    "switch": {
      "child_lock": { "name": "Child lock" }
    },
    "sensor": {
      "current_temperature": { "name": "Current temperature" },
      "power": { "name": "Power" },
      "energy": { "name": "Energy" }
    }
  }
}
